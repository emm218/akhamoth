/// sums the numbers in a list
pub fn sum(l: [int]) -> int {
    sum(l, 0b99)
}

fn sum([h | t]: [int], acc: int) -> int {
    Io.println("meow :3");
    sum(t, acc + h)
}

fn sum([]: [int], acc: int) -> int {
    acc
}
